"""Generated documentation, distinct from the per-repo wiki.

The wiki answers "what is this repository" in one page. These answer narrower questions in
their own documents, and each is built from the graph rather than written by a model:

- ``api`` -- the interface reference: public symbols, signatures, and their REAL call sites.

Two more are planned and deliberately not stubbed here, because an empty module reads as a
feature that exists: design documents from the graph, and a per-repo onboarding guide.
"""
