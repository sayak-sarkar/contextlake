---
name: Bug report
about: Something isn't working the way it should
title: "bug: "
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**
What you thought would happen instead.

**To reproduce**
The exact command you ran and the steps to reproduce:

```bash
gitlab-sync ...
```

**Output**
Paste the relevant output. ⚠️ Scrub any private group names, repo paths, URLs,
or tokens first.

```
...
```

**Environment**
- `gitlab-sync --version`:
- Python version (`python --version`):
- `glab --version`:
- OS:

**Additional context**
Anything else that might help — config (redacted), how many repos, etc.
